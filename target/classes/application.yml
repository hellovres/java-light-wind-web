spring:
  application:
    name: lightwind-auth

server:
  port: 8080

# Session 配置
session:
  timeout: 3600  # 1 hour (seconds)

logging:
  level:
    com.lightwind.web: DEBUG
    org.springframework.security: DEBUG
